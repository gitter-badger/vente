import React from 'react'

/**
* This is the component which wraps the login buttons
* @class LoginElement
* @classdesc This class wraps the different login buttons and handles their actions (facebook, google, github or simple sign up). 
* @author Mayas Haddad
*/
export default class CreateNewLead extends React.Component {
    
    /**
    **  @name constructor
    **/
   constructor(props) {
      super(props)
    }
   
   /**
    * renders the different login buttons
    * @returns the JSX representing the login element.
    */
   render() {
      return (
       <div>
          Hello World!
       </div>
      )
    }
}